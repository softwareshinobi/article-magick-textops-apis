version: '3.8'

services:

    article-magick-api:

        container_name: article-magick-api
        
        image: softwareshinobi/article-magick-api:2.4

        build: 

            context: .

            dockerfile: Dockerfile

        restart: always

        ports:

            - "8888:8888"
